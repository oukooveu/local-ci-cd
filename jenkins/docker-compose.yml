version: '3.7'
services:
  jenkins-master:
    image: 'jenkins/jenkins:lts'
    hostname: jenkins
    domainname: lo
    ports:
      - '8080:8080'
      - '8443:8443'
      - '50000:50000'
    volumes:
      - '$PWD/data:/var/jenkins_home'
    networks:
      jenkins:
        aliases:
          - jenkins.lo
volumes:
  jenkins_data:
    driver: local
networks:
  jenkins:
    name: localci
